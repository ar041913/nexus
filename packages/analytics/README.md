# Analytics

Internal Python analytics core for NEXUS.ai. All quantitative truth is computed here — never in the LLM.

Submodules follow the pipeline: ingest → reconcile → kpi → detect → explain → ground → confidence → actions → simulate → learn.
