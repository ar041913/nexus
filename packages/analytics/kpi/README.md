# KPI Engine

Executes versioned KPI semantic contracts via deterministic SQL in DuckDB.

Future responsibilities:
- Contract registry loading
- Parameterized SQL execution
- KPI observation persistence with lineage
