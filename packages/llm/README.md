# LLM

Provider-independent LLM adapter for narrative synthesis only. The LLM must never be the source of quantitative truth.

Future responsibilities:
- Structured prompt → validated JSON narrative
- Numeric cross-check validation
- Token and cost tracking
