# Database

Connectors for DuckDB (analytical) and PostgreSQL (application state), plus future migration utilities.

Future responsibilities:
- DuckDB connection management
- PostgreSQL connection pool
- Schema migrations
