# Scripts

Utility scripts for seeding databases and running the analytics pipeline.

Scripts will be added in Phase 1+:
- `seed_db.py` — load NovaMart fixtures
- `run_pipeline.py` — CLI pipeline runner
